{{{
  exports({
    to: app.configPath('./', 'socket.ts')
  })
}}}
import { defineConfig } from 'adonisjs-socket-io'
import env from '#start/env'

export default defineConfig({
    defaultGuard: env.get('SOCKET_DEFAULT_GUARD'),
    socketKernelPath: '#start/socket',
    socketIoOptions: {
        cors: {
            origin: '*'
        }
    }
})